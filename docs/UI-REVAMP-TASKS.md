# Agent implementation packets

Initial delivery completed on 2026-09-22. These packets remain the scope boundaries for later refinements. Final presentation rules live in `app/src/studio.css`, imported after the original `style.css`, to keep the stages reviewable. See UI-REVAMP-REVIEW.md for actual evidence; do not rerun completed packets without new feedback or a regression.

Gemini 3.8 Flash High drafted these packets through AGY; root reviewed them against actual source. Corrected its invented `src/__tests__` paths, removed speculative new counters/milestones and narrowed broad test runs. All commands below run in `app/`. Follow UI-REVAMP-PLAN.md acceptance and credit policy. One writer; preserve existing uncommitted work.

For every packet: inspect its listed functions and reference sections, make only scoped changes, run `npm run build` plus listed checks after code changes, record results and next action in UI-REVAMP-CONTINUATION.md. Do not claim browser behavior from unit tests alone. Add tests only for new meaningful behavior.

| Packet / dependency | Files and implementation steps | Acceptance / focused checks |
| --- | --- | --- |
| P0 / none | Record git status; inspect package scripts and relevant tests. Run baseline build and navigation/preview tests once. Capture current demo shell if browser available. | Record pre-existing failures separately. `npx vitest run src/nav-access.test.ts src/preview-gateway.test.ts`. |
| P1 / P0 | `style.css`: map root/light and theme-dark tokens, typography, controls and panels. `App.tsx`: TopBar and application shell only if markup needs adjustment. Preserve theme persistence, existing route handlers and role-gated nav. Adapt reference header/sidebar to mobile. | Build; navigation/preview tests above if markup changes. Visually inspect both themes and mobile menu, Escape, focus return, Settings access. No hidden essential links. |
| P2 / P1 | `App.tsx`: PageHome, ObligationRow, InitiativeCard; corresponding scoped CSS. Group real Roast Me obligations and reviews first, then decisions, threads and initiatives. Reuse current selectors/callbacks. Add restrained noninteractive neural decoration only if it does not obscure content. | Counts/links match current data; signed-out/pending home branches remain usable; empty states retained. `npx vitest run src/modal-workflows.test.tsx src/nav-access.test.ts`; desktop/mobile visual inspection and provisional taste score. |
| P3a / P2 | `App.tsx`: PageCatalog, PageInitiative, InitiativeCard; CSS. Restyle search/filter controls, tabs, task list and metadata. Preserve selection and join actions; do not invent milestones. | Search/filter/detail links and task actions work. `npx vitest run src/preview-gateway.test.ts src/initiative-tasks.test.ts`. |
| P3b / P3a | `App.tsx`: DocumentModal, DraftEditor, SubmittedDoc, SplitPane and form presentation; CSS. Keep editor DOM, callbacks, autosave timers and dirty-close guards intact. Style toolbar, validation, revision/media and comment surfaces. Avoid editing Editor.tsx behavior. | Keyboard dialog workflow, save/reload, unsaved-close, submission, revision, pasted media and narrow split view checked. `npx vitest run src/modal-workflows.test.tsx src/revision-media.test.ts`. Run rm-cycle tests only if submission/cycle code changed. |
| P3c / P3b | `App.tsx`: PageSettings, PageSignIn, profile presentation, PageAccounts, PageAssignments, PageHealth, PageAudit, PageNotifications, PageProposals, PageRequests; CSS. Mostly shared control/table styling; preserve guard conditions and actions. | Member/lead/Research/Operations views retain allowed actions and deny forbidden ones. Tables scroll locally on mobile. `npx vitest run src/nav-access.test.ts src/remaining-ux.test.ts`. |
| P4 / P1, scheduled after P3c | Add small `app/src/WorkspaceLoader.tsx`; `main.tsx`: replace only Root loading branch; `style.css`: scoped loader animation/reduced motion. Translate supplied SVG into React attributes, unique gradient IDs. Keep pending driven by existing state; no fabricated progress/telemetry or minimum wait. | SVG aria-hidden plus readable role=status; no motion in reduced-motion mode; actual success exits, actual failure shows existing alert/retry. Preserve auth refresh logic. Build and a focused loader integration test with mocked pending/success/error if introducing a test harness is small; otherwise explicitly record manual state evidence. Existing remaining-ux tests do not prove loader transitions. |
| P5 / P2–P4 | Review only changed files and screenshots; fix concrete acceptance failures. Update UI-CHANGES.md and continuation record. | Build and one focused integrated run: `npx vitest run src/nav-access.test.ts src/preview-gateway.test.ts src/modal-workflows.test.tsx src/remaining-ux.test.ts src/initiative-tasks.test.ts src/revision-media.test.ts`. Reuse checks already run on final unchanged files. Browser at 390/768/1440px, both themes, reduced motion, representative role/empty/error states. Mark unavailable evidence unverified. |

## Agent prompt template

Implement packet [ID] only. Read UI-REVAMP-PLAN.md, this packet, the relevant UI references and listed source sections. Use the current app and real data. Preserve behavior, permissions, hash routes and other working-tree changes. No new framework, backend changes, bulk refactor or unrelated cleanup. Run the specified focused checks, inspect relevant responsive states if available, and report exact files/results/limitations. Update UI-REVAMP-CONTINUATION.md before ending. Stop at the configured allowance threshold. Do not commit or deploy.
