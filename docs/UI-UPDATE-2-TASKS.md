# Updated visuals — agent packets

All paths are repository-relative. Root integrates drafts sequentially. Use the high-level plan and only the specified reference/source context. No broad App.tsx rewrite or stylesheet duplication.

| Packet | Files and concrete steps | Acceptance / validation |
| --- | --- | --- |
| U0 baseline | Inspect updated reference files and git diff. Reuse last verified unchanged app baseline; query current allowance. | Baseline commit 479c885; no pre-existing source edits. |
| U1 identity/background | Copy logo SVG to assets and favicon; TopBar uses local image. Create a small NeuralBackground component or isolated SVG asset from supplied neural-web markup. Add scoped `visual-update.css`, import after studio.css, light/dark opacity and pointer-events:none. Hide-tab/reduced-motion cleanup. | Both themes legible, no scroll overflow or blocked clicks; no WebGL dependency. Build plus browser shell checks. |
| U2 loader | Port supplied canvas script to typed `connectingIdeas.ts` with `(canvas, dark) => cleanup`. Keep original seeded branching, cap DPR/frame rate; handle no context, resize, motion preference and visibility; cleanup all resources. WorkspaceLoader owns only a canvas ref/effect and current theme. Shared tiny theme initializer if needed; update App initializer without changing persistence. | Lifecycle test remains green; fake-canvas test verifies no RAF leak and reduced-motion/hidden-tab pauses; browser confirms nonblank dark/light renderer. No minimum loading delay. |
| U3 catalog | App.tsx PageCatalog plus catalog-specific card component/function; preserve shared InitiativeCard behavior elsewhere. Search title/category/abstract and lead name; derive categories from permitted records, keep public active-only gate. Real result count; simple name sort only if adding sort. Supplied local SVG illustration fallbacks, existing cover URLs/crops preferred. CSS media reveal on hover/focus, visible touch metadata. | Test category/search/active/public filtering and keyboard/touch reachability; browser on desktop/mobile/light/dark; no fake counts or nested buttons inside links. |
| U4 integration | Update browser fixture/checker to inspect new canvas and background instead of old SVG loader; meaningful animation and catalog tests. Save screenshots/report and update continuation. | Build, relevant existing nav/preview/modal tests, new catalog/animation tests, responsive browser checks. Actual results only. |

For each handoff include: changed files, intended behavior, checks executed, limitations, next packet and allowance. Stop after two repeated failures at the same boundary; save a concrete continuation instead of an open-ended retry loop. Old helper output JSON is draft provenance, not final source.
